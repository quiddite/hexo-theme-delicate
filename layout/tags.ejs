<% 
    page.layout = "tags";
    page.title = __("tags");

    let min_font = 15,
      max_font = 15,
      unit = 'px',
      amount = 900,
      start_color = "#70f3ff",
      end_color = "#70f3ff";
%>

<section class="tags">
    <h1><%= site.tags.length %> tags in total</h1>
    <div class="tags-list">
        <%- tagcloud({
            min_font: min_font,
            max_font: max_font,
            amount: 999,
            unit: unit,
            color: true,
            start_color,
            end_color
        }) %>
    </div>
</section>